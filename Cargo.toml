[package]
name = "timeline-system"
version = "0.1.0"
edition = "2021"
description = "A lightweight, functional, branching timeline system with causal propagation and self-correcting world tendencies"
license = "MIT"

[dependencies]
uuid = { version = "1", features = ["v4", "serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
# integration tests use the main lib directly
